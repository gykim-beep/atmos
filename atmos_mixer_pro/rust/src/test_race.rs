use crate::core::state::GLOBAL_STATE;
use crate::api::simple::*;

pub fn run_stress_test() {
    println!("Starting stress test...");
    // Just a placeholder, we can't easily run it if the app isn't running.
}
